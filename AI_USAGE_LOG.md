# AI Usage Log

This assignment explicitly expects and wants visible AI usage (§0.3 of the brief). This
log documents how Claude was used across the session, what was generated vs. reviewed vs.
fixed, and why.

## Log

| Stage | Task | Generated / Edited / Rejected | Rationale                                                                                                                                                                                                   |
|---|---|---|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Scaffold | Initial project design: hash-chain scheme, domain model, service layers, controller, tests, docs | Generated by Claude as a reference design | Used as the working design; rebuilt file-by-file in IntelliJ myself, layer by layer, rather than copy-pasting a finished tree, so I made sure Claude understand my requirements                             |
| Setup | Maven/JDK/Docker environment setup, git init, GitHub repo creation, branch workflow | Followed step-by-step guidance | Ran every command myself, verified each result before proceeding                                                                                                                                            |
| Bug: package structure | Layers ended up nested incorrectly (`com.schwab.dto` instead of `com.schwab.auditlog.dto`) | Diagnosed together after `mvn compile` misbehaved; fixed via IntelliJ's Move refactor | I ran the diagnostic commands, applied the fix, and verified with `mvn compile` before committing                                                                                                           |
| Bug: empty entry point | `AuditLogApplication.java` was missing its `@SpringBootApplication` annotation and `main` method | Diagnosed from the exact Maven error ("Unable to find main class"); restored the file | Verified root cause via `find`/`cat` before applying the fix                                                                                                                                                |
| Bug: timestamp precision | `/audit/verify` flagged untouched records as tampered after a restart | Diagnosed by comparing POST vs. GET responses for the same record; fixed by truncating the timestamp to milliseconds before hashing | Reproduced the false positive first, applied the fix, then reproduced correct tamper detection to confirm the fix, all against a real running MongoDB                                                       |
| Testing | Unit tests (Hashing, AuditLogService, ChainVerificationService), Swagger config, Postman collection | Generated, then run and reviewed | All 11 unit tests run and confirmed passing locally before each commit                                                                                                                                      |
| Integration test | Testcontainers-based integration test | Generated; attempted to run repeatedly | Could not get it running due to a Docker Desktop version compatibility issue on this machine; documented as a known limitation (`docs/ARCHITECTURE.md` §9) rather than left unexplained or silently deleted |
| Docs | README, ARCHITECTURE, SCENARIO_A/B/C, FINAL_SUMMARY | Generated in collaboration, based on my own decisions about scope (e.g. which Scenario C interpretation to pursue) | Reviewed and gave my ideas for each scenariors to Claude and Claude documented                                                                                                                              |

